import { MenuDisablePipe } from './menu-disable.pipe';

describe('MenuDisablePipe', () => {
  it('create an instance', () => {
    const pipe = new MenuDisablePipe();
    expect(pipe).toBeTruthy();
  });
});

import { ZoomDisablePipe } from './zoom-disable.pipe';

describe('ZoomDisablePipe', () => {
  it('create an instance', () => {
    const pipe = new ZoomDisablePipe();
    expect(pipe).toBeTruthy();
  });
});
